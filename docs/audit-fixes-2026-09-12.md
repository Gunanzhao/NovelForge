# 2026-09-11 审查报告修复记录

范围：本地 `NovelForge-audit-continuation-2026-09-11.md` 所列 NF-01～NF-08。基线为 `2f30376`，版本号保持 1.1.1-rc.6；本轮未发布新 Release。报告中的后续功能建议不属于这八项缺陷。

## 修复

| 编号 | 结果 | 对应提交 |
| --- | --- | --- |
| NF-01 | 删除无关节点保留当前正文、选区和保存状态；删除当前章节及父级期间禁止编辑受影响正文，先保存并创建保护快照；失败后恢复编辑。无当前文档时仍选择首章。 | `fcc7169`、`0657540` |
| NF-02 | 使用同目录单次 rename 替换目标，移除旧文件先移走的空档；旧版 project.json 中断副本保留并显示具体恢复路径。 | `0c4178a` |
| NF-03 | 后端基于规范化实际路径和格式白名单决定打开方式；脚本、程序、快捷方式、网页和未知格式只在文件管理器定位。 | `d2979df` |
| NF-04 | 创建前检查引用，创建后、校验和恢复共同验证清单约束、内容哈希、项目元数据、数据库及正文/资料/回收站引用。失败备份清理，不输出“校验成功但无法恢复”的结果。 | `f3ff44f` |
| NF-05 | 远程 Markdown 图片默认占位；用户点击后才生成图片请求，权限绑定当前图片与项目会话。 | `e0244f2` |
| NF-06 | 从生产 handler 移除 save_document 和 rename_node；保留内部实现及浏览器演示兼容，桌面脚本改用 checked 保存。 | `0ffd180` |
| NF-07 | 冲突读取集中到 Store，核对请求代次、项目、章节、正文版本及错误状态；取消后的迟到响应不替换正文，读取前保护当前内存内容。 | `bbfb4a4` |
| NF-08 | 打开请求各自持有锁令牌；后端错误自动回收本次请求，前端未接纳的目标在 finally 中释放；取消关闭时恢复当前令牌，不释放其他会话所有权。 | `6caf9cf` |

## 验证

- 前端：63 个测试文件、407 项测试通过；包含删除等待期间输入、切换章节、冲突读取取消/重复/错误、交错打开以及保存失败清理。
- Rust：全量 134 项通过、7 项按配置跳过。包括真实子进程锁探测，合法 ZIP/哈希但缺失引用的拒绝，以及完整备份恢复重开。
- 文件替换并发回归：200 次替换、10,000 次读取，没有目标缺失或半写入内容。
- 类型检查、ESLint、Rustfmt、Clippy、Windows Tauri release EXE 和 NSIS 安装包构建通过。
- `scripts/reliability-ui-cdp.mjs`：旧 IPC 拒绝、外部图片点击前零请求/点击后一次请求、冲突保护和读取、完整备份恢复、草稿保护及撤销通过。
- `pnpm test:e2e:desktop` 完整工作流通过：编辑/树操作、模板、自动快照故障恢复、重启持久化、AI 离线与本地模拟 Provider、回收站恢复、六种导出。修正了旧脚本的路径选择器、模板入口和 AI 控件；历史故障注入改为等待自动快照，因为手动保存不写历史。
- `pnpm audit --audit-level high` 未发现已知漏洞；本机未安装 cargo-audit，Rust 依赖审计由仓库 CI 执行。

## 验证边界

- 未执行真实断电试验；单次替换提供目标可见性的原子性，不承诺所有文件系统和硬件上的断电持久性。
- 迟到响应覆盖由可控 Promise 的 Store 回归验证；尝试的桌面 IPC 延迟夹具未成功拦截，不计为该场景的桌面验收通过。桌面正常冲突恢复另行通过。
- 危险附件使用路径/扩展名策略测试，未执行危险文件；格式限制不代替用户对来源的判断。
- 使用隔离项目与合成内容；没有使用真实稿件或消耗真实 AI 额度。
