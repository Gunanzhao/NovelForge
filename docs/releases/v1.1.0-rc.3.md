# NovelForge v1.1.0-rc.3 发布说明草案

状态：**未发布；当前开发候选目标，候选源码完成、本地产物已验收，源码基线 CI 已通过**。

既有已发布版本为 v1.1.0-rc.2，安装包 `NovelForge_1.1.0-rc.2_x64-setup.exe` 的说明见 [rc.2 历史发布说明](v1.1.0-rc.2.md)。当前工作区源码可能包含后续变更，不等同于该安装包；rc.3 本地产物已生成并验收，未作为 Release 资产发布。

## 修复目标

- **ISSUE-01（Wiki 统计）**：区分识别建议与统计语义。Mention Inspector 不重复提示已有 Wiki；统计应计入已知人物、地点和世界观的 Wiki mention。普通文本与 Wiki 混合时精确累计，未知 Wiki 不生成已知资料计数，代码区 Wiki 仍排除。修复已完成，全部新增回归已纳入 rc.3 前端 34 文件 / 221 项测试并通过；三种桌面 E2E 均已完整通过。
- **ISSUE-02（Markdown 边界）**：统一 Markdown protected-range 处理；fence opener 长度至少 3，closer 字符相同且长度不小于 opener，多 backtick inline code 按相同 delimiter 长度闭合，未闭合 fence 保护到文末。保留 URL、链接/图片目标、Wiki 和 frontmatter 边界，核对与 Markdown 字符转换 helper 的一致性。修复已完成，全部新增回归已纳入 rc.3 前端 34 文件 / 221 项测试并通过；三种桌面 E2E 均已完整通过。

## 验证与发布门禁

- 前端 frozen-lockfile 安装、typecheck、lint 通过；测试 34 文件 / 221 项全部通过（25.20 秒）；pnpm audit 无已知漏洞。前端 build 已在 Tauri beforeBuild 中通过（tsc + Vite，18.03 秒）。
- Rust check/fmt/Clippy `-D warnings` 通过；常规测试 74 passed / 2 ignored（6.78 秒），ignored 基准已单独通过；Cargo audit 已通过（exit 0，17 条 allowed warnings，与 rc.2 相同）。
- 1000 章 / 100 万字和 V1.1 辅助数据两项 benchmark：rc.3 实测通过，分别为 42.180 秒 / 22.605 秒；采样范围及比较限制见 TEST_REPORT。
- Windows 完整 Tauri release 构建已通过；CDP 已完整通过，WebDriver 第二轮完整通过，WebDriver + Native Dialog 完整通过（exit 0）。CDP 已通过六个 V1.1 标记及 `WIKI_MENTION_COUNT_OK`；WebDriver 也已完整通过；Native Dialog 完整通过（exit 0）。
- main 分支保护与 Required Checks 已生效，配置见 RELEASE_CHECKLIST；rc.3 源码基线 CI 已通过。
- 版本文件与 EXE/NSIS ProductVersion 均为 rc.3；产物大小及 SHA-256 见 TEST_REPORT。
- rc.3 tag/Release/安装包发布：未执行；保留既有 rc.1/rc.2 历史 tag 与发布记录。

完整验证记录见 [TEST_REPORT](../../TEST_REPORT.md#rc3-validation)，交付记录见 [RELEASE_CHECKLIST](../../RELEASE_CHECKLIST.md#rc3-checklist)。rc.3 候选源码与本地产物已完成验收，源码基线 CI 已通过；本次不创建 rc.3 Release 或 tag。

## 兼容性

延续 rc.2 的普通 Markdown 正文与资料镜像兼容性说明。rc.3 目标是在本地可重建索引和 Markdown 扫描边界上修正行为；项目打开、恢复及既有流程已随三种桌面完整回归通过；这些验证结论对应当前 rc.3 本地产物。

## rc.3 当前交付状态

rc.3 候选源码完成、本地产物已验收，源码基线 CI 已通过。本次范围为修复与推送，不创建 rc.3 tag 或 Release，不上传发布资产；已发布安装包仍为 `NovelForge_1.1.0-rc.2_x64-setup.exe`。

源码基线 `0f1eb3f8756a5936491f483c783387598b01a3d7`（`fix/v1.1-audit-rc3`）已推送；[CI run 33966324453](https://github.com/Gunanzhao/NovelForge/actions/runs/33966324453) 为 `completed / success`，Frontend checks 与 Rust checks 均为 `completed / success`。全分支 push 触发已由该 run 验证。

上述 CI 只证明该源码基线。后续文档提交仍须按受保护 main 规则重新运行并通过检查，再 fast-forward 更新 main；不将源码 CI 视为尚未产生的文档提交或 main 最终提交的验证结果。

本地测试、两项基准、三种桌面 E2E、产物摘要及历史 tag 证据见 [TEST_REPORT](../../TEST_REPORT.md#rc3-validation) 与 [RELEASE_CHECKLIST](../../RELEASE_CHECKLIST.md#rc3-checklist)。rc.2 tag 已核验未移动，main 保护规则已生效。
