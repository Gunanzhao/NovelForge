# rc.5 修复与验收记录

日期：2026-09-06。基线为 rc.4 审计，历史问题报告保留在 `../rc4/AUDIT_2026-09-06.md`。本记录区分自动测试、实际安装验收和未覆盖范围。

## 修复对应关系

审计 01–11 均已实现修复；另补充本节点和父级正文锁定。阶段提交为 `6a7e99b`（保存与会话）、`7a018fb`（恢复/导出/锁定）、`da80e1c`（资料/参数/主题/布局）。详细状态见 `FIX_PROGRESS.md`。

## 当前执行结果

| 检查 | 结果 |
|---|---|
| 前端类型检查、Lint、生产构建 | 通过 |
| Vitest | 40 文件，264 项通过 |
| Rust 常规测试 | 94 项通过，5 项标记需显式运行 |
| Rust 显式 CLI/订阅测试 | 3 项通过：ChatGPT 鉴权、空工具列表、真实生成/哨兵/取消 |
| Rust 千章百万字测试 | 通过，37,532 ms |
| Rust 大辅助数据测试 | 通过，20,509 ms |
| Rust 格式与 Clippy | 通过 |
| pnpm audit | 没有已知漏洞 |
| cargo audit | 退出 0，17 条既有允许警告；不能称为零安全警告 |
| Windows NSIS 构建与安装 | rc.5 安装到专用验收目录，安装程序退出 0 |
| 安装版 CDP 完整流程 | 编辑/树操作、资料、Wiki、设置、恢复、规划、搜索、AI Provider、回收站、六种导出通过 |
| 视口与主题 | 1440×900、1366×768、1100×650 共 51 张截图；全部 bodyWidth 等于视口宽度；系统深色变化生效 |
| 原生关闭与锁定 | 输入/格式操作不能改变锁定正文；WM_CLOSE 保存失败保留窗口及恢复文本；排除故障后重试保存退出 0 |

真实订阅使用 CLI 0.149.1 / gpt-5.6-luna，合成续写40、润色23、分析64、模板73字符，哨兵保护和取消通过。没有使用真实小说测试。

## 安装产物追溯

安装后程序版本为 1.1.0-rc.5，18,067,456 字节，SHA-256：`89eea4976560ff94e3d43d707d129cb03856d8e394d4ad31060207f8a0fa28d8`。

构建目录中的 EXE 与安装后的 EXE 只有偏移 13704442–13704444 的三个字节不同：`UNK` → `NSS`。已对照 Tauri utils 2.9.3 的 `platform.rs`，这是 `__TAURI_BUNDLE_TYPE_VAR_NSS` 安装类型标记，对应 NSIS。不能直接把两个原始哈希声明为相同。安装包的独立 SHA-256 见 Release 附件。

## 证据和边界

- `layout-metrics.json`、`screenshots/`：全部页面与导出弹窗的几何测量及截图；代表性最小窗口正文、Codex、导出截图人工复核。滚动区域下方内容或弹窗背后的按钮不视为被遮挡缺陷。
- `close-evidence.json` 和 `screenshots/native-close-protected.png`：原生关闭、恢复文本及重试退出证据。合成测试项目保留于记录路径，未触碰用户小说。
- 附件旧数据库仍存在时自动补齐镜像；数据库与镜像都已缺失时只能恢复原文件，不能恢复丢失的说明/关联。
- 没有重新穷举所有 Windows DPI/缩放、左右栏最大宽度组合、Linux/macOS、第三方 Provider、模型及外部 DOCX/EPUB/PDF 阅读器的视觉兼容性。本轮六种导出生成和语义测试通过，不等于所有阅读器视觉验收。
- 17 条 Rust 警告包括 GTK3 停止维护、glib unsound 和 Tauri 传递依赖维护状态；与既有策略一致，没有隐藏或删除警告。
- 本记录不把历史 rc.4 验收当作 rc.5 当前结果，也不声称已证明不存在其他漏洞。远程 CI 和发布资产将在最终推送后另行核验。
