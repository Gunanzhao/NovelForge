# 全量审查修复验收（2026-09-12）

本轮审查确认的五项缺陷均已修复，每项在回归验证后单独提交。

| 问题 | 修复 | 提交 |
| --- | --- | --- |
| FA-01 备份漏检历史及回收站后代文件 | 创建、校验、恢复共用完整引用检查；按删除批次解析章节小节位置，检查文件类型 | 7af29c5 |
| FA-02 自动保存重置导出选项 | 同一对话框会话保留选择，关闭重开或项目/预设变化时重新初始化 | 2d0f2be |
| FA-03 DOCX 编号未关联正文 | 始终写入 numbering 关系，有封面时同时保留图片关系 | fde067f |
| FA-04 Enter 重复创建节点 | 同步锁阻止重复请求，处理期间禁止关闭，失败保留标题并允许重试 | 4e70b32 |
| FA-05 EPUB 每卷重复封面 | 仅首个正文分段包含封面 | 10c0932 |

验证结果：

- 前端：65 个测试文件、410 项测试通过；typecheck、lint、生产构建通过。
- Rust：137 项通过、7 项按原设置忽略；fmt、clippy（警告视为错误）通过。
- 桌面：通过 Tauri CLI 构建 release EXE；完整 desktop E2E 通过，覆盖编辑、历史、资料、设置、恢复、规划、搜索、AI 取消、回收站和六种导出。
- 新增隔离验收：`node scripts/full-audit-acceptance-cdp.mjs` 检查缺失历史/回收站章节被拒绝，补回后的备份、校验、恢复及读取成功。
- `python scripts/check-audit-exports.py` 验证真实 DOCX 的编号关系、EPUB 阅读顺序仅一张封面。两条命令顺序执行，证据写入忽略目录 `tmp/full-audit-acceptance/`。
- 备份回归还覆盖先删章节再删卷、章节侧目录、伪造一致哈希但遗漏小节的压缩包，以及用目录替换正文文件。

本轮未升级依赖。此前审查的六项间接依赖维护提示不等同于漏洞，仍需随上游升级跟踪；未执行消耗订阅额度的真实 AI 请求。构建存在原有的动态导入分包提示；桌面回归的一条裁剪诊断不影响用例通过。
