# NovelForge 构建任务文档

## 1. 项目名称

**NovelForge**

一款面向中文长篇小说创作的、本地优先的 Markdown 桌面写作软件。

目标不是制作一个普通 Markdown 编辑器，而是构建一套完整的小说创作工作台，将以下能力整合到一个桌面应用中：

- Markdown 正文写作
- 卷 / 章 / 节管理
- 人物资料
- 地点资料
- 世界观 Wiki
- 大纲与场景卡
- 时间线
- 伏笔管理
- 全文搜索
- 写作统计
- 自动保存与版本历史
- AI 辅助
- 小说一致性检查
- 多格式导出
- 插件扩展

最终产品定位：

> Scrivener + Obsidian + Markdown Editor + 小说设定管理器 + AI Assistant

---

# 2. 开发原则

本项目必须遵守以下原则。

## 2.1 本地优先

正文和核心资料必须默认保存在用户本地。

不得要求用户注册账号才能正常使用。

不得依赖云端服务才能完成：

- 创建小说
- 写作
- 编辑
- 搜索
- 人物管理
- 世界观管理
- 导出

---

## 2.2 Markdown 优先

所有正文必须保存为普通 `.md` 文件。

禁止将正文仅存储在 SQLite 数据库中。

即使 NovelForge 被卸载，用户仍应能够直接打开项目中的 Markdown 文件。

---

## 2.3 数据安全优先

任何情况下都应尽可能避免用户正文丢失。

必须实现：

- 自动保存
- 临时文件
- 原子写入
- 崩溃恢复
- 回收站
- 历史版本
- 异常退出恢复

---

## 2.4 模块化

所有大型功能必须拆分为独立模块。

禁止将所有业务逻辑集中到单一文件或单一组件。

---

## 2.5 可测试

每个主要功能必须具有明确验收方式。

能自动测试的部分必须尽可能编写自动测试。

---

# 3. 推荐技术栈

默认技术栈：

```text
Desktop:
Tauri 2

Frontend:
React
TypeScript
Vite

UI:
Tailwind CSS
shadcn/ui

Editor:
CodeMirror 6

State:
Zustand

Database:
SQLite

Backend:
Rust

Markdown:
remark
rehype

Search:
SQLite FTS5

Package Manager:
pnpm
```

如果某项依赖与 Tauri 2 存在明显兼容性问题，可以更换实现方案，但必须：

1. 在 `DECISIONS.md` 中记录原因。
2. 不得擅自整体切换至 Electron。
3. 不得修改核心产品目标。

---

# 4. 目标平台

第一阶段：

```text
Windows 11 x64
```

后续保留：

```text
Linux
macOS
```

的兼容空间。

不得大量使用只能在 Windows 工作、且无法抽象的前端业务代码。

---

# 5. 项目目录

推荐最终结构：

```text
NovelForge/
├─ src/
│  ├─ app/
│  ├─ components/
│  ├─ editor/
│  ├─ features/
│  │  ├─ manuscript/
│  │  ├─ characters/
│  │  ├─ locations/
│  │  ├─ world/
│  │  ├─ timeline/
│  │  ├─ outlines/
│  │  ├─ scenes/
│  │  ├─ foreshadowing/
│  │  ├─ search/
│  │  ├─ statistics/
│  │  ├─ history/
│  │  ├─ export/
│  │  ├─ ai/
│  │  └─ plugins/
│  ├─ hooks/
│  ├─ lib/
│  ├─ stores/
│  ├─ types/
│  └─ utils/
│
├─ src-tauri/
│  ├─ src/
│  │  ├─ commands/
│  │  ├─ filesystem/
│  │  ├─ database/
│  │  ├─ search/
│  │  ├─ backup/
│  │  ├─ export/
│  │  └─ lib.rs
│  └─ tauri.conf.json
│
├─ tests/
├─ scripts/
├─ docs/
│
├─ SPEC.md
├─ TODO.md
├─ PROGRESS.md
├─ DECISIONS.md
├─ CHANGELOG.md
├─ TEST_REPORT.md
└─ README.md
```

---

# 6. 用户小说项目结构

NovelForge 创建的每本小说采用以下格式：

```text
MyNovel/
├─ project.json
│
├─ manuscript/
│  ├─ volume_001/
│  │  ├─ chapter_001.md
│  │  ├─ chapter_002.md
│  │  └─ chapter_003.md
│  └─ volume_002/
│
├─ characters/
├─ locations/
├─ world/
├─ timeline/
├─ outlines/
├─ scenes/
├─ foreshadowing/
├─ notes/
├─ research/
├─ attachments/
├─ trash/
│
└─ .novelforge/
   ├─ database.sqlite
   ├─ history/
   ├─ recovery/
   ├─ cache/
   └─ index/
```

---

# 7. 主界面

默认采用三栏布局。

```text
┌─────────────────────────────────────────────────────────────┐
│ NovelForge  项目 编辑 视图 工具 AI 设置                    │
├──────────────┬──────────────────────────┬───────────────────┤
│              │                          │                   │
│ 项目导航      │      Markdown编辑区       │      辅助栏       │
│              │                          │                   │
│ 正文          │                          │ 当前章节          │
│ 人物          │                          │ 人物              │
│ 地点          │                          │ 地点              │
│ 世界观        │                          │ 标签              │
│ 时间线        │                          │ 大纲              │
│ 伏笔          │                          │ AI                │
│ 资料库        │                          │                   │
│              │                          │                   │
├──────────────┴──────────────────────────┴───────────────────┤
│ 字数 | 今日新增 | 保存状态 | 当前文件                     │
└─────────────────────────────────────────────────────────────┘
```

左右栏必须支持：

- 折叠
- 调整宽度
- 保存布局状态

---

# 8. Phase 1：基础框架

优先完成可运行骨架。

## 任务

- [ ] 初始化 Tauri 2 项目
- [ ] React + TypeScript
- [ ] Vite
- [ ] Tailwind CSS
- [ ] shadcn/ui
- [ ] Zustand
- [ ] SQLite
- [ ] ESLint
- [ ] Prettier
- [ ] Vitest
- [ ] 基础错误处理
- [ ] 应用日志系统
- [ ] Windows 开发构建

## 验收

执行：

```bash
pnpm install
pnpm tauri dev
```

必须能够正常启动桌面窗口。

---

# 9. Phase 2：小说项目管理

实现：

```text
创建小说
打开小说
最近项目
关闭项目
项目设置
```

项目基本信息：

```text
作品名
作者
简介
类型
创建时间
最后修改时间
目标字数
```

必须支持最近项目列表。

---

# 10. Phase 3：正文结构

支持层级：

```text
作品
└─ 卷
   └─ 章
      └─ 节
```

用户必须可以：

- 创建卷
- 创建章节
- 创建节
- 重命名
- 删除
- 拖拽排序
- 移动章节
- 复制章节
- 批量选择

删除内容不得立即永久删除。

统一移动至：

```text
trash/
```

---

# 11. Phase 4：Markdown 编辑器

采用 CodeMirror 6。

必须支持：

- Markdown
- 标题
- 粗体
- 斜体
- 删除线
- 引用
- 无序列表
- 有序列表
- 任务列表
- 分割线
- 链接
- 图片
- 表格
- 脚注

提供：

```text
Markdown模式
即时预览模式
编辑/预览分栏
```

---

# 12. 中文写作优化

加入：

- 首行缩进
- 中文标点检查
- 中英文标点转换
- 全角 / 半角转换
- 多余空格清理
- 空白行清理
- 连续标点提示

这些功能不得未经用户确认自动破坏正文。

---

# 13. 自动保存

必须重点实现。

建议流程：

```text
编辑器
 ↓
内存状态
 ↓
临时文件
 ↓
原子替换
 ↓
正式Markdown
 ↓
创建历史快照
```

必须考虑：

- 程序崩溃
- 强制关闭
- 文件写入失败
- 磁盘空间不足
- 文件被其他程序占用

界面显示：

```text
正在保存
已保存
保存失败
存在恢复数据
```

---

# 14. 崩溃恢复

应用启动时检查：

```text
.novelforge/recovery/
```

如果发现未完成数据，应提示：

```text
检测到未恢复的写作内容。
```

允许：

```text
恢复
查看差异
忽略
删除恢复文件
```

---

# 15. 人物系统

人物字段：

```text
姓名
别名
性别
年龄
生日
身份
职业
阵营
头像

外貌
性格
能力
弱点
习惯

背景
目标
动机
秘密

首次登场
当前状态
备注
```

支持：

```text
自定义字段
标签
搜索
排序
筛选
```

---

# 16. 人物关系

人物之间可以创建关系：

```text
朋友
敌人
家人
师生
同事
恋人
暗恋
自定义
```

数据结构必须支持：

```text
人物A
关系
人物B
备注
```

后期支持人物关系图。

---

# 17. 名字生成器

右侧工具栏增加：

```text
名字生成器
```

类型：

- 人物
- 地点
- 国家
- 城市
- 组织
- 公司
- 物品
- 武器
- 技能
- 功法
- 舰船
- 星球

风格：

```text
中文现代
中文古风
武侠
仙侠
日式
欧美
西方奇幻
科幻
```

支持：

```text
批量生成
复制
收藏
重新生成
直接创建对应条目
```

第一版可以使用本地规则生成。

禁止强制依赖 AI API。

---

# 18. 地点管理

地点支持树状层级。

例如：

```text
大陆
└─ 国家
   └─ 城市
      └─ 王宫
```

地点字段：

```text
名称
类型
所属地点
描述
人口
气候
历史
势力
重要人物
重要事件
相关章节
标签
图片
```

---

# 19. 世界观 Wiki

支持用户创建自由 Wiki 条目。

分类示例：

```text
国家
种族
宗教
历史
政治
经济
军事
科技
魔法
文化
组织
语言
物品
生物
```

Markdown 正文必须支持：

```text
[[人物名]]
[[地点名]]
[[世界观条目]]
```

点击打开对应条目。

必须支持双向链接查询：

```text
哪些章节引用了这个条目
```

---

# 20. 大纲系统

支持：

```text
作品大纲
卷大纲
章节大纲
```

章节可记录：

```text
章节目标
主要冲突
重要事件
出场人物
地点
结果
备注
```

---

# 21. 场景卡

每章可以创建多个 Scene。

结构：

```text
Scene标题

POV
地点
时间
参与人物

目标
冲突
结果
备注
```

场景可以拖拽排序。

---

# 22. 看板

章节支持以下状态：

```text
未开始
草稿
初稿
修改中
完成
锁定
```

提供 Kanban：

```text
未开始      草稿       修改中       完成

第8章       第6章       第4章       第1章
第9章       第7章       第5章       第2章
```

允许拖动改变状态。

---

# 23. 时间线系统

每个事件包括：

```text
标题
日期
时间
描述
参与人物
地点
关联章节
标签
```

必须支持普通日期。

架空历法后续实现。

时间线至少提供：

```text
列表视图
时间排序
人物筛选
地点筛选
章节筛选
```

---

# 24. 伏笔管理

字段：

```text
名称
说明
首次埋设章节
计划回收章节
实际回收章节
状态
标签
备注
```

状态：

```text
计划中
已埋设
部分回收
已回收
废弃
```

提供：

```text
未回收伏笔
长期未处理伏笔
按章节筛选
```

---

# 25. 全文搜索

搜索范围：

```text
正文
人物
地点
世界观
大纲
时间线
伏笔
笔记
```

使用：

```text
SQLite FTS5
```

支持：

- 即时搜索
- 关键词高亮
- 当前文档搜索
- 全项目搜索
- 区分大小写
- 类型过滤
- 卷过滤
- 标签过滤

快捷键：

```text
Ctrl+F
Ctrl+Shift+F
```

---

# 26. 快速打开

快捷键：

```text
Ctrl+P
```

搜索：

```text
章节
人物
地点
Wiki条目
```

例如输入：

```text
林月
```

立即打开人物资料。

---

# 27. 命令面板

快捷键：

```text
Ctrl+Shift+P
```

支持：

```text
创建章节
创建人物
创建地点
打开章节
生成名字
进入专注模式
导出
切换主题
```

架构必须允许其他模块注册命令。

---

# 28. 写作统计

统计：

```text
总字数
当前卷
当前章节
今日
昨日
本周
本月
```

额外统计：

```text
连续写作天数
平均每日字数
最长写作天数
```

目标：

```text
每日目标
项目目标
```

例如：

```text
267412 / 300000
```

---

# 29. Dashboard

首页显示：

```text
作品名称
总字数
今日字数
连续写作
最近章节
写作目标
最近修改
未处理伏笔
```

避免过度复杂。

---

# 30. 专注模式

F11 或快捷按钮进入。

隐藏：

```text
菜单
左侧栏
右侧栏
工具栏
```

可选：

```text
打字机模式
当前行高亮
宽度限制
背景主题
```

---

# 31. 主题

至少提供：

```text
浅色
深色
跟随系统
```

允许单独调整：

```text
字体
字号
行高
内容宽度
段落间距
```

---

# 32. 版本历史

保存章节历史快照。

例如：

```text
18:32 自动保存
17:58 修改对话
16:42 初稿
```

必须支持：

```text
查看
Diff
恢复
复制旧版本
```

历史文件存放：

```text
.novelforge/history/
```

---

# 33. 回收站

删除：

```text
章节
人物
地点
Wiki
```

时进入回收站。

用户可以：

```text
恢复
永久删除
清空回收站
```

---

# 34. 资料库

项目中支持保存：

```text
Markdown
TXT
图片
PDF
DOCX
其他附件
```

第一版只要求：

```text
导入
分类
搜索
打开
关联章节
```

无需第一阶段解析所有格式。

---

# 35. AI 模块

AI 必须是可选功能。

软件没有 API Key 时必须完全正常使用。

接口统一采用 OpenAI-compatible API。

支持配置：

```text
名称
Base URL
API Key
Model
Temperature
Max Tokens
```

预留：

```text
OpenAI
DeepSeek
OpenRouter
本地模型
其他兼容服务
```

---

# 36. AI 功能

实现：

```text
续写
润色
改写
扩写
缩写
章节摘要
生成大纲
角色对话
设定建议
名字生成
```

---

# 37. AI 上下文系统

禁止默认发送整本小说。

用户必须明确选择上下文。

可选：

```text
当前选中文字
当前段落
当前章节
最近N章
指定人物
指定地点
指定世界观
指定笔记
```

UI 应显示：

```text
将发送给AI的内容
```

---

# 38. AI 请求安全

API Key：

不得明文写入：

```text
project.json
普通配置JSON
日志
```

优先使用系统安全存储。

至少保证日志不会输出完整 API Key。

---

# 39. 小说一致性检查

先构建规则检查框架。

第一阶段检测：

```text
相同人物多个年龄
相同人物多个生日
死亡人物后续出现
人物名称疑似拼写变化
人物性别描述冲突
地点名称疑似变化
时间线顺序问题
```

结果必须标记为：

```text
可能存在问题
```

禁止直接声称一定是错误。

---

# 40. AI 一致性检查

后续可以将：

```text
人物资料
地点资料
章节摘要
剧情事件
```

提供给 AI 分析。

但必须与规则检查模块解耦。

---

# 41. 导出系统

第一阶段：

```text
Markdown
TXT
```

第二阶段：

```text
HTML
DOCX
EPUB
PDF
```

支持：

```text
整本
指定卷
指定章节
```

可以配置：

```text
作品名
作者
目录
卷标题
章节标题
封面
```

---

# 42. Markdown 合并

必须支持：

```text
将整个小说合并成单个Markdown
```

顺序严格按照：

```text
卷排序
章节排序
节排序
```

---

# 43. 快捷键

至少实现：

```text
Ctrl+N       新建
Ctrl+S       保存
Ctrl+B       粗体
Ctrl+I       斜体
Ctrl+F       搜索
Ctrl+Shift+F 全文搜索
Ctrl+P       快速打开
Ctrl+Shift+P 命令面板
F11          专注模式
```

快捷键架构应允许未来自定义。

---

# 44. 插件系统

插件系统放在后期开发。

目标：

```text
插件能够注册：

命令
菜单
右侧栏
工具
导出器
生成器
```

不要在第一阶段过早实现复杂插件运行时。

首先设计清晰接口。

---

# 45. 性能要求

目标：

打开：

```text
1000章节
1,000,000+中文字
```

的小说项目时仍能够正常使用。

不得一次将所有 Markdown 正文全部加载到 React DOM。

文件树必须考虑虚拟化。

搜索必须建立索引。

---

# 46. 大型文件处理

单章节：

```text
100,000字
```

仍应能够打开和编辑。

编辑器不得明显卡死。

---

# 47. UI 要求

总体风格：

```text
现代
克制
桌面生产力软件
```

参考：

```text
VS Code
Obsidian
Notion
Scrivener
```

但不得直接复制。

避免：

```text
大量渐变
巨大圆角
无意义动画
过度卡片化
```

重点是：

```text
信息密度
清晰层级
快速操作
```

---

# 48. 响应式布局

主要目标是桌面窗口。

最低建议窗口：

```text
1280 × 720
```

在较小窗口时：

```text
允许折叠左右侧栏
```

---

# 49. 数据模型

所有核心实体必须拥有：

```text
id
created_at
updated_at
```

ID 使用：

```text
UUID
```

不得依赖名称作为唯一标识。

---

# 50. 数据库职责

SQLite 主要用于：

```text
元数据
索引
人物
地点
时间线
伏笔
关系
标签
统计
搜索
```

正文：

```text
Markdown文件
```

数据库损坏不得导致正文文件无法读取。

---

# 51. project.json

建议：

```json
{
  "formatVersion": 1,
  "id": "uuid",
  "title": "小说名称",
  "author": "",
  "description": "",
  "targetWords": 300000,
  "createdAt": "",
  "updatedAt": ""
}
```

必须保留：

```text
formatVersion
```

用于未来迁移。

---

# 52. 日志

实现分级日志：

```text
DEBUG
INFO
WARN
ERROR
```

禁止日志记录：

```text
API Key
完整隐私内容
整个小说正文
```

---

# 53. 错误处理

所有文件 IO 必须处理错误。

禁止：

```text
unwrap()
expect()
```

大量出现在 Rust 生产代码路径。

用户能够理解的错误需要展示友好的错误提示。

---

# 54. 自动测试

至少覆盖：

```text
项目创建
项目打开
章节创建
章节删除
章节恢复
文件保存
自动保存
搜索
人物CRUD
地点CRUD
世界观CRUD
时间线CRUD
伏笔CRUD
Markdown导出
项目导出
```

---

# 55. 测试层级

使用：

```text
Unit Test
Integration Test
E2E Test
```

优先测试核心数据安全功能：

```text
自动保存
恢复
删除
导出
```

---

# 56. 数据安全测试

必须模拟：

```text
保存过程中中断
目标文件被占用
损坏恢复文件
重复打开项目
无写入权限
```

至少保证程序不会直接造成原文件被清空。

---

# 57. 开发文档

持续维护：

```text
README.md
SPEC.md
TODO.md
PROGRESS.md
DECISIONS.md
CHANGELOG.md
TEST_REPORT.md
```

---

# 58. TODO.md 规则

每项任务：

```text
- [ ] 未开始
- [~] 进行中
- [x] 完成
- [!] 阻塞
```

示例：

```text
- [x] 初始化Tauri
- [x] 创建项目
- [~] Markdown编辑器
- [ ] 自动保存
```

禁止一次将大量未经验证的任务标记完成。

---

# 59. PROGRESS.md

每完成一个阶段更新：

```text
完成内容
修改文件
测试结果
已知问题
下一步
```

---

# 60. DECISIONS.md

遇到架构决策时记录：

```text
问题
可选方案
最终选择
原因
影响
日期
```

不得仅在对话中保存重要技术决策。

---

# 61. TEST_REPORT.md

必须记录：

```text
测试时间
构建环境
单元测试
集成测试
E2E
失败测试
已知问题
```

---

# 62. Git 工作流

每完成一个相对独立功能：

```text
检查代码
运行测试
提交Git
```

Commit 示例：

```text
feat(editor): implement markdown editor
feat(character): add character management
fix(save): prevent data loss during atomic write
test(project): add project creation tests
```

禁止：

```text
update
fix things
changes
```

等无意义 commit。

---

# 63. 开发流程

每个任务严格按照：

```text
读取SPEC
 ↓
检查TODO
 ↓
确认当前代码状态
 ↓
实现一个功能
 ↓
运行测试
 ↓
修复测试
 ↓
更新TODO
 ↓
更新PROGRESS
 ↓
提交Git
 ↓
继续下一任务
```

---

# 64. 禁止行为

Codex 不得：

1. 为了快速完成而删除已有功能。
2. 跳过明显失败的测试。
3. 修改测试使错误代码通过。
4. 用大量 Mock 假装功能已经实现。
5. 在 UI 中只放占位按钮然后标记完成。
6. 大规模重写已经稳定工作的模块。
7. 擅自更换整体技术栈。
8. 删除用户数据。
9. 将 Markdown 正文改为数据库专有格式。
10. 未验证即声明 Phase 完成。

---

# 65. 阶段验收要求

一个 Phase 只有同时满足：

```text
功能实现
+
程序可运行
+
相关测试通过
+
TODO更新
+
PROGRESS更新
```

才可以标记完成。

---

# 66. MVP 范围

第一版必须完成：

```text
✓ 小说项目创建/打开
✓ 卷/章管理
✓ Markdown编辑
✓ 自动保存
✓ 崩溃恢复
✓ 人物管理
✓ 地点管理
✓ 世界观
✓ Wiki链接
✓ 全文搜索
✓ 写作统计
✓ 专注模式
✓ Markdown导出
✓ TXT导出
✓ 回收站
✓ 基础版本历史
```

达到以上要求即可视为：

```text
NovelForge MVP
```

---

# 67. V0.2

加入：

```text
章节大纲
场景卡
时间线
伏笔
看板
人物关系
命令面板
名字生成器
```

---

# 68. V0.3

加入：

```text
AI Provider
AI续写
AI润色
AI改写
AI摘要
AI上下文选择
```

---

# 69. V0.4

加入：

```text
一致性检查
DOCX导出
EPUB导出
PDF导出
资料库
高级统计
```

---

# 70. V1.0

目标：

```text
稳定
数据安全
完整小说工作流
优秀性能
Windows安装包
```

同时：

```text
0个Critical Bug
主要自动测试通过
测试恢复流程
完成完整小说项目测试
```

---

# 71. Codex 首次启动任务

首先不要立即编写大量业务代码。

按照以下顺序执行：

## Step 1

检查当前目录。

如果项目不存在：

```text
初始化 Git
```

---

## Step 2

创建：

```text
SPEC.md
TODO.md
PROGRESS.md
DECISIONS.md
CHANGELOG.md
TEST_REPORT.md
```

将本任务书核心要求整理进：

```text
SPEC.md
```

---

## Step 3

初始化：

```text
Tauri 2
React
TypeScript
Vite
Tailwind
shadcn/ui
Zustand
SQLite
Vitest
```

---

## Step 4

确保：

```bash
pnpm install
pnpm tauri dev
```

能够正常运行。

---

## Step 5

实现最基本应用框架：

```text
顶部菜单
左侧导航
主编辑区域
右侧辅助栏
底部状态栏
```

---

## Step 6

运行测试并提交：

```text
Initial application shell
```

---

## Step 7

从：

```text
小说项目管理
```

开始进入功能开发。

---

# 72. 自主开发要求

在没有需要用户明确决策的问题时：

**不要因为完成一个小任务就停止。**

应继续：

```text
读取TODO
选择下一项
实现
测试
记录
继续
```

直到：

```text
遇到真正阻塞
或
本轮开发目标完成
```

---

# 73. 遇到问题时

优先：

```text
阅读错误
查看日志
检查官方文档
检查依赖源码/类型
编写最小测试
定位问题
```

不得在首次失败后立即放弃实现。

---

# 74. 代码质量

TypeScript：

```text
避免any
使用明确类型
组件职责单一
业务逻辑抽离
```

Rust：

```text
Result错误传播
清晰模块边界
避免panic
```

React：

```text
避免巨大组件
避免无意义useEffect
避免重复状态
```

---

# 75. 性能原则

避免：

```text
每次输入都写SQLite
每次输入都扫描整个小说
每次切换章节重新建立完整索引
一次性渲染所有章节
```

合理使用：

```text
debounce
background task
incremental indexing
virtualized list
cache
```

---

# 76. 最终验收场景

创建测试小说：

```text
10卷
每卷100章
总计1000章
```

包含：

```text
100个人物
100个地点
200个世界观条目
500个时间线事件
100个伏笔
```

正文规模：

```text
≥ 1,000,000中文字
```

验证：

```text
启动
打开项目
搜索
切换章节
编辑
自动保存
人物管理
Wiki跳转
导出
关闭
重新打开
崩溃恢复
```

---

# 77. 数据安全最终测试

执行模拟：

```text
编辑章节
 ↓
突然终止程序
 ↓
重新启动
```

必须能够：

```text
恢复最新编辑内容
```

或者：

```text
最多只损失极短时间内未落盘内容
```

不得损坏原章节。

---

# 78. 完成定义 Definition of Done

任何任务只有满足以下所有条件才算完成：

```text
实现真实功能
可以实际操作
无明显占位实现
编译成功
测试通过
无新增严重错误
更新TODO
更新PROGRESS
```

---

# 79. 当前首要目标

当前不要追求：

```text
AI
高级动画
插件市场
云同步
账号系统
多人协作
```

第一目标是：

> 构建一个稳定、安全、真正能够写长篇小说的本地 Markdown 创作软件。

优先级：

```text
数据安全
>
写作体验
>
项目管理
>
搜索与资料组织
>
高级功能
>
AI
```

---

# 80. 给 Codex 的执行指令

从现在开始执行 NovelForge 项目开发。

首先完整阅读本文件，不要跳过要求。

然后：

1. 检查当前仓库状态。
2. 创建或更新 `SPEC.md`。
3. 创建详细 `TODO.md`。
4. 将任务拆成可以独立验证的小任务。
5. 初始化项目。
6. 完成 Phase 1。
7. 运行构建与测试。
8. 修复发现的问题。
9. 更新 `PROGRESS.md`。
10. 自动继续开发下一阶段。

开发过程中持续维护：

```text
SPEC.md
TODO.md
PROGRESS.md
DECISIONS.md
CHANGELOG.md
TEST_REPORT.md
```

每完成一个功能必须：

```text
实现
→
编译
→
测试
→
修复
→
记录
→
继续
```

不要通过删除测试、降低验收标准或使用占位实现来制造“完成”。

如果 SPEC 与当前实现发生冲突：

```text
优先保证数据安全
其次遵守SPEC
然后在DECISIONS.md记录处理方式
```

目标不是尽快生成大量代码，而是持续构建一个可以真正使用的 NovelForge。